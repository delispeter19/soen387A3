open in IntelliJ as a project

ensure that the json and mysql connector dependencies are installed 
(see a3\src\main\webapp\WEB-INF\lib for correct dependencies)

configure tomcat server to run project on "http://localhost:8080/a3_war"

	- do not open on launch --> if it does open on launch, ignore the splash page

lastly make sure to add your <root> and <password> configurations in the Database.java file.